# linux-lit to-do

Running list of reader bugs and feature requests. Mark completed items with a
leading `[X]`; never delete them.

'Return' should be bound to what 'a' is bound in main card.

read Gloss ID: 21810 and Gloss ID: 21807. both liken Julia to "fishing" and "angling."
when creating glosses, first consult any existing glosses for source text adjacent to the
current source text to be glossed. refrain from recycling the same metaphors, images or
other rhetorical devices used by existing glosses of neighboring source text.

/home/mlj/Screenshots/screenshot-2026-07-20_12-19-28.png the 'rr' chord bind in main card
for 2-col layout should pop up a vocab pop up that complements 2-col layout instead of
showing the vocab definitions in one of the two columns. pressing escape in the pop up
should close it. also create a version of the ctrl+alt+backslash bind for 2-col layout so
the user can add vocab words and their definitions are automatically added to lit.db.

like works shown in the main card, the glosses and journal overlays and their panels in
the chat panel should highlight vocab words in lit.db and allow the user to toggle the
vocab pop up for showing definitions using the 'rr' chord. (this requires reassigning the
'r' and 'R' binds in the overlays and chat panel.) so also should the overlays and chat
panel provide the keybind ctrl+alt+backslash for adding a vocab word to lit.db and its
definition should automatically be added if neither are in lit.db)

when chat panel is visible there needs to be an indicator either above the chat panel or
above the main card that indicates it has focus. probably a horizontal rule in the header
that is about the width of three hyphens.
