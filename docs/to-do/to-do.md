# linux-lit to-do

Running list of reader bugs and feature requests. Mark completed items with a
leading `[X]`; never delete them.

'o' and 'e' binds in reader, with playback sync toggled on, works much better in prose
works with phrase timestamps than in MM. what can be done for verse works so that 'o' and
'e' and other navigation binds take full advantage of phrase timestamps like in prose
works? begin with MM-Arkangel, since it is the only verse work with phrase timestamps
currently
