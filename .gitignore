# All dev logs incl. per-instance-slot suffixes (-2, -3, ...) and stderr tees
linux-lit-dev*.log
linux-lit-release.log
linux-lit-stderr.log
target/
.superpowers/

# NRC-VAD lexicon — redistribution-restricted academic license; fetch locally,
# do not commit. See scripts/build_embeddings.py header for the download URL.
scripts/data/NRC-VAD-Lexicon.txt

# Claude Code session transcripts & auto-memory (local, not project source)
.claude/projects/
# Claude Code runtime lock (per-process scheduled-tasks lock; not source)
.claude/scheduled_tasks.lock
CLAUDE-activeContext.md
# Transient plan file (per CLAUDE.md, plans go here; never source)
PLAN.md

# Superpowers brainstorm/SDD session artifacts (mockups, ledgers; not source)
.superpowers/
